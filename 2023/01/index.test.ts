import { describe, expect, test } from 'bun:test';

describe('part 1', () => {
  test('example', () => {
    expect(1).toBe(1);
  });
});

describe('part 2', () => {
  test('example', () => {
    expect(1).toBe(1);
  });
});
